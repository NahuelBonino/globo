using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class globo_move : MonoBehaviour {


    private Rigidbody2D rigi;
    private Transform trans;
    //float cont;
    //float cont_velocidad;

    private float velocidadGlobo;



	void Start () {
       /* rigi = GetComponent<Rigidbody2D>();
        trans = GetComponent<Transform>();
        
        
        if (Crtl_Objetivos_lvl1.getInstance().getCurrentObj() == 1){
            rigi.AddForce(new Vector2(0f, velocidadGlobo),UnityEngine.ForceMode2D.Impulse);

        }
       else {

           rigi.AddForce(new Vector2(0f, velocidadGlobo),UnityEngine.ForceMode2D.Impulse);

       }*/
      
       // print(rigi.velocity.y);
   }
	
	// Update is called once per frame
}
